{
  "name": "{{PROJECTNAME}}",
  "version": "1.0.0",
  "description": "{{DESCRIPTION}}",
  "main": "index.js",
  "scripts": {
    "start": "micro -l tcp://0.0.0.0:${PORT-8080}",
    "dev": "micro-dev . -p 8080",
    "test": "standard --verbose | snazzy && tape test/api/test.js | tap-summary",
    "lint": "standard --fix"
  },
  "author": "{{USERNAME}}",
  "repository": "{{USERNAME}}/{{PROJECTNAME}}",
  "license": "MIT"
}
