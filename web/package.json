{
  "name": "{{PROJECTNAME}}",
  "description": "{{DESCRIPTION}}",
  "author": "{{USERNAME}}",
  "version": "0.1.0",
  "main": "index.js",
  "private": true,
  "files": [
    "index.js"
  ],
  "scripts": {
    "test": "standard --verbose | snazzy",
    "start": "bankai index.js -A 0.0.0.0 -p 8080 -a .",
    "build": "NODE_ENV=production bankai build --optimize dist/"
  },
  "repository": "{{USERNAME}}/{{PROJECTNAME}}",
  "license": "MIT"
}
